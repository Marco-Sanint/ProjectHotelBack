# Backend Environment Variables
# Copy this file to .env and fill in the values
# IMPORTANT: Never commit .env to version control

# Environment
NODE_ENV=production

# Server Configuration
PORT=3000

# JWT Secret Key
# Generate a secure random string for production (e.g., using: openssl rand -base64 32)
SECRET_KEY=your-secret-key-here

# CORS Allowed Origins
# Comma-separated list of allowed origins (no spaces after commas)
# Example: https://your-app.vercel.app,https://your-custom-domain.com
ALLOWED_ORIGINS=https://your-app.vercel.app,https://hotelregatta.site

# Database Configuration (if using AWS RDS or external database)
# Uncomment and configure if not using SQLite local
# DB_HOST=your-db-host.rds.amazonaws.com
# DB_PORT=5432
# DB_NAME=hotel_db
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
